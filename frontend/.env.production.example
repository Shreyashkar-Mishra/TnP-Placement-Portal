# Production Environment Variables
# These should be set in Vercel's Environment Variables dashboard, not pushed to Git!

# The URL of your Render backend API (do not include trailing slash)
VITE_API_BASE_URL=https://your-backend-url.onrender.com/api/v1
